"""Bundled integration files (skills / commands / rules) for AI coding agents.

These are installed into each agent's config directory by `podpull skills install`
(see podpull.skills). They are packaged as data files inside the wheel.
"""
